# SCGK114 图片工作台部署日志

日期：2026-08-11

## 交付范围

- 公开入口：`https://scgk114.com/image/`
- 实际工具页：`https://api.scgk114.com/image`
- 面向普通访问者的页面不要求登录，不展示 API Key、Base URL、上游服务、账号池、用量、余额或价格。
- 未访问、修改或复用任何非 SCGK114 项目的账号、用户数据、容器、域名或配置。

## 架构

```text
浏览器 /image
  -> SCGK114 图片服务 /api/image/*
  -> SCGK114 服务端账号池与路由
  -> OpenAI 兼容 Images API
  -> 同源短期图片预览/下载地址
```

浏览器只请求 SCGK114 的相对接口；前端不保存、不读取、也不发送长期凭据。模型由服务端白名单固定为 `gpt-image-2`。

## 已部署组件

- 独立容器：`scgk-image-web`
- 服务目录：`/opt/scgk-image`
- Docker 网络：`sub2api_sub2api-network`
- 服务端配置仅保存在服务器私有环境文件中，不记录在仓库或本文档中。
- Caddy 已增加 `/image*` 与 `/api/image*` 路由，Caddy 改动前备份：`/opt/sub2api/Caddyfile.bak-image-20260811`。
- 图片下载令牌与临时结果清单存放在独立数据卷中；服务重启后，未过期的下载链接仍可使用。

## 页面与接口

- `GET /image`
- `POST /api/image/generate`
- `GET /api/image/tasks/{taskId}`
- `GET /api/image/files/{opaqueToken}`
- `GET /api/image/files/{opaqueToken}?download=1`

前端支持提示词、尺寸、质量、数量、生成状态、本次会话历史、预览和下载。模型选择、服务商和账户相关字段均不对客户端开放。

## 当前策略

- IP：15 分钟最多 4 次生成请求。
- 匿名访客标识：15 分钟最多 3 次生成请求。
- 单访客：同时最多 1 个进行中任务。
- 全局并发：1 个；队列上限：6 个。
- 请求体上限：64 KiB；提示词上限：2,000 字符；每次生成 1 至 2 张。
- 单任务上游超时：180 秒。
- 结果下载有效期：60 分钟。
- 失败信息均使用中文脱敏提示，不返回上游原始错误、账号状态或通道信息。

## 验收记录

- `https://api.scgk114.com/image`：HTTP 200。
- `https://api.scgk114.com/api/image/health`：HTTP 200。
- 已以未登录普通访问者身份完成一次真实文生图任务，任务状态为 `completed`。
- 该任务的图片预览和下载接口均返回 HTTP 200。
- 重启 `scgk-image-web` 后，再次访问同一临时图片链接仍返回 HTTP 200。
- 空提示词返回中文校验提示，不含上游报错细节。
- 前端源码与请求层已检查，不包含长期 Key、`Authorization` 请求头、上游地址或账号池标识。
- 页面 DOM 验证了提示词、尺寸、质量、数量、生成按钮和本次会话结果区均正常渲染。

限流、并发和队列由服务端配置与代码路径启用；为避免无意义消耗共享额度，未通过连续真实生成请求刻意打满队列。

## 发布记录

- `e13f6cc` Add isolated SCGK image generation service
- `35676f1` Avoid charging duplicate image task submissions
- `d6ad675` Connect image service to existing Sub2API network
- `8d92fc3` Accept unstripped image API routes
- `da102b1` Redirect public image route to isolated service
- `eb72dc9` Preserve temporary image downloads across service restarts

## 回滚

仅回滚图片工作台，不影响已有 SCGK114 API、账号池或其他容器：

```bash
cd /opt/scgk-image
docker compose -f image-service/docker-compose.image.yml --env-file image-service/.env down
cp /opt/sub2api/Caddyfile.bak-image-20260811 /opt/sub2api/Caddyfile
docker exec sub2api-caddy caddy reload --config /etc/caddy/Caddyfile --adapter caddyfile
```

如需恢复图片工作台，在 `/opt/scgk-image` 拉取已验证提交后重新构建：

```bash
git checkout eb72dc9
docker compose -f image-service/docker-compose.image.yml --env-file image-service/.env up -d --build
```

